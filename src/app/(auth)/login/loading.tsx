import LoadingIcon from '@/components/__icons/Loading.Icon';

function Loading() {
  return (
    <section className="flex items-center justify-center text-primary">
      <LoadingIcon />
    </section>
  );
}

export default Loading;
